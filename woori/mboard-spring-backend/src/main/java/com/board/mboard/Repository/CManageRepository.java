package com.board.mboard.Repository;

import com.board.mboard.Entity.CManage_tbl;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CManageRepository extends JpaRepository<CManage_tbl, String> {
}
