package com.example.article.Repository;
/*
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Query;
import org.springframework.stereotype.Repository;

import java.util.List;

@Repository
public interface 참고Repository extends JpaRepository<Entity이름, 기본키데이터형> {
    List<Entity명> findBy필드명(데이터형 변수);

    @Query(select u from 테이블명 u where u.필드 = :변수명)
    List<Entity명> 사용자메소드(데이터형 변수);

    @Query(update 테이블명 u set u.필드명=값 where u.필드 = :변수명)
    List<Entity명> 사용자메소드(데이터형 변수, 데이터형 값);

    @Query(insert 테이블명 u(u.필드명...) values(:값,....))
    void 사용자메소드(데이터형 값...);
    @Query(deleteu from 테이블명 u where u.필드 = :변수명)
    void 사용자메소드(데이터 변수명);

}
*/